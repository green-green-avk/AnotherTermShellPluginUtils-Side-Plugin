dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = 'Another Term Shell Plugin Utils - Side - Plugin'
include ':anothertermshellpluginutils-side-plugin'
